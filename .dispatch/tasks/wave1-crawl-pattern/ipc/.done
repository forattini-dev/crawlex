wave1-crawl-pattern complete.

Owner ÚNICO files touched:
- src/scheduler.rs       (+~310 lines; InterArrivalJitter, JitterProfile,
                          SessionDepthTracker, SessionDecision,
                          WeightedFrontier, frontier_weight,
                          DEFAULT_FRONTIER_WEIGHTS, BudgetLimits.max_per_session_total)
- src/crawler.rs         (wire inter_arrival + session_depth into process_job;
                          emit decision.made on Pareto cap; requeue on EndSession)
- src/http/cookies.rs    (NEW — PartitionedCookieStore w/ CHIPS parsing +
                          SameSite=None; Secure enforcement + partition isolation)
- src/http/mod.rs        (NEW — pub mod cookies)
- src/lib.rs             (pub mod http)
- tests/crawl_pattern_shape.rs (NEW — 8 distribution-shape tests)

Consequent small edits (additive only, no semantic change):
- src/cli/mod.rs                    (..Default::default() on BudgetLimits literal)
- tests/render_budgets.rs           (..Default::default() on literals)
- tests/throughput_live.rs          (..Default::default() on literal)
- .dispatch/tasks/wave1-crawl-pattern/plan.md  (checklist [x])

NOT touched: stealth_shim, motion, pool.rs, handler, impersonate, antibot.
Chrome 149 patches preservados. Licenças preservadas. No commits made.

Gates:
- cargo build --bin crawlex-mini --no-default-features --features cli,sqlite   → exit 0
- cargo check --lib --no-default-features --features cli,sqlite                → exit 0
- cargo clippy --lib --no-default-features --features cli,sqlite               → exit 0
                                  (warnings: 6 pre-existentes em outros owners)
- cargo test --test crawl_pattern_shape --no-default-features --features cli,sqlite
                                  → 8/8 passed
- cargo test --test render_budgets --no-default-features --features cli,sqlite
                                  → 6/6 passed (sem regressão)
- cargo build --all                → FAIL; errors em src/render/motion/submovement.rs
                                     (u01/next_u32 / rand 0.10 API drift) —
                                     pre-existente, OUTRO owner (motion).
                                     Meu diff não introduz nenhum erro de build;
                                     clippy/tests nos arquivos owned passam.
- Live HN: não executado nesta worktree — stealth_shim/pool intactos,
           paths de budget/dispatch não alterados além do jitter sleep
           opcional (Off em motion=fast, que é o preset do HN throughput
           live test → zero overhead preservado).

Items plan:
- #31 InterArrivalJitter                           DONE
- #32 click-graph shape (hub-spoke weighting)      DONE (helper + tests)
- #33 session depth Pareto cap                     DONE
- #23 CHIPS cookies                                DONE
- #26 IndexedDB audit                              SCOPE-OUT
     (observer lives in render/spa_observer.rs — owner externo;
      não tocado conforme restrição "não tocar handler/pool")

Artifacts: see plan.md checklist and tests/crawl_pattern_shape.rs.
